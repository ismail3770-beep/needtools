const fs=require('fs');const t=fs.readFileSync('src/config/toolsRegistry.ts','utf8');const d=fs.readFileSync('src/components/tools/ToolDispatcher.tsx','utf8');const tIds=[...t.matchAll(/id:\s*['\"]([^'\"]+)['\"]/g)].map(m=>m[1]);const dIds=[...d.matchAll(/case\s*['\"]([^'\"]+)['\"]:/g)].map(m=>m[1]);console.log('Registry IDs:', tIds); console.log('Dispatcher IDs:', dIds); console.log('In Registry not in Dispatcher:', tIds.filter(id=>!dIds.includes(id)));
